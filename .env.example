# Base URL for all API endpoints (all routes are prefixed with /api)
# Used in Zustand stores for data fetching (events, tickets, auth, etc.)
VITE_API_URL=http://localhost:4000/api

# Base URL for non-API backend services (e.g., WebSocket connections, auth callbacks)
# Do NOT include the /api prefix here
VITE_BACKEND_URL=http://localhost:4000